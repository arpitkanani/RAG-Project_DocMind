# **docMind** 

### setup
- create python envrionment by running below command
    - python -m venv venv python=3.10
    -venv/Scripts/activate
- after that install dependencies library by running following command
    - pip install -r reqirements.txt

